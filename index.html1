import { useState } from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export default function TradingStatsTracker() {
  const [trades, setTrades] = useState([]);
  const [form, setForm] = useState({
    date: "",
    pair: "",
    direction: "",
    rr: "",
    result: "",
    notes: "",
  });

  const addTrade = () => {
    if (!form.date || !form.pair || !form.direction || !form.rr || !form.result) return;
    setTrades([...trades, form]);
    setForm({ date: "", pair: "", direction: "", rr: "", result: "", notes: "" });
  };

  const totalTrades = trades.length;
  const wins = trades.filter(t => t.result.toLowerCase() === "win").length;
  const losses = trades.filter(t => t.result.toLowerCase() === "loss").length;
  const winRate = totalTrades > 0 ? ((wins / totalTrades) * 100).toFixed(1) : 0;

  return (
    <div className="min-h-screen bg-gray-100 p-6">
      <h1 className="text-3xl font-bold mb-6 text-center">Trading Stats Tracker</h1>
      <Card className="max-w-3xl mx-auto p-4 shadow-lg">
        <CardContent>
          <div className="grid grid-cols-2 gap-4">
            <Input placeholder="Date" value={form.date} onChange={e => setForm({ ...form, date: e.target.value })} />
            <Input placeholder="Pair" value={form.pair} onChange={e => setForm({ ...form, pair: e.target.value })} />
            <Input placeholder="Direction (Buy/Sell)" value={form.direction} onChange={e => setForm({ ...form, direction: e.target.value })} />
            <Input placeholder="R:R" value={form.rr} onChange={e => setForm({ ...form, rr: e.target.value })} />
            <Input placeholder="Result (Win/Loss)" value={form.result} onChange={e => setForm({ ...form, result: e.target.value })} />
            <Input placeholder="Notes" value={form.notes} onChange={e => setForm({ ...form, notes: e.target.value })} />
          </div>
          <Button className="mt-4 w-full" onClick={addTrade}>Add Trade</Button>
        </CardContent>
      </Card>

      <div className="max-w-4xl mx-auto mt-8">
        <h2 className="text-xl font-semibold mb-2">Stats</h2>
        <div className="flex gap-6 mb-4">
          <p>Total Trades: {totalTrades}</p>
          <p>Wins: {wins}</p>
          <p>Losses: {losses}</p>
          <p>Win Rate: {winRate}%</p>
        </div>

        <h2 className="text-xl font-semibold mb-2">Trade History</h2>
        <table className="w-full border border-gray-300">
          <thead>
            <tr className="bg-gray-200">
              <th className="p-2 border">Date</th>
              <th className="p-2 border">Pair</th>
              <th className="p-2 border">Direction</th>
              <th className="p-2 border">R:R</th>
              <th className="p-2 border">Result</th>
              <th className="p-2 border">Notes</th>
            </tr>
          </thead>
          <tbody>
            {trades.map((trade, i) => (
              <tr key={i} className="text-center">
                <td className="p-2 border">{trade.date}</td>
                <td className="p-2 border">{trade.pair}</td>
                <td className="p-2 border">{trade.direction}</td>
                <td className="p-2 border">{trade.rr}</td>
                <td className={`p-2 border ${trade.result.toLowerCase() === "win" ? "text-green-600" : "text-red-600"}`}>{trade.result}</td>
                <td className="p-2 border">{trade.notes}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}
